#include<bits/stdc++.h>
using namespace std;

int main()
{
    int x = 10, y = 20;

    // max function...
    // cout << max(x, y) << endl;
    // cout <<"Minimun value : " <<min(x, y) << endl;

    // squareroot function...
    // int z = 25;
    // cout << sqrt(z) << endl;

    // double sum = 25.55;     // round function...
    // int result = round(sum);
    // cout << result << endl;
     
    int xy = -5;
    int result = abs(xy);
    cout << "Absoluate vlue is : " << result << endl;


    return 0;
}