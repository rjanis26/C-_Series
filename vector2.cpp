/* #include<bits/stdc++.h>
using namespace std;

int main(){

    vector<int> v;
    vector<int>::iterator it;

    v.push_back(1);
    v.push_back(3);
    v.push_back(55);
    v.push_back(6);

    cout<< v.at(0) << endl;
    cout << "------------" << endl;
    
    for(it = v.begin(); it != v.end(); it++){
        cout << *it << "\t";
    }

    puts(" ");

    cout <<"Size of the vector: " << v.size() << endl;
    cout << "fron of vector: "<< v.front() << endl;
    cout << "Back of vertor:" << v.back() << endl;
     


    return 0;
    
} */

/* #include<bits/stdc++.h>
using namespace std;

int main(){

    vector<int> v;
    v.push_back(11);
    v.push_back(34);
    v.push_back(44);
    v.push_back(5);
    v.push_back(22);

    //v.pop_back();
    //v.pop_back();

    for(int i  = 0; i < v.size(); i++){
        cout << v[i] << "\t";
    }
    cout << endl;

    cout<<"Size of the vector: " << v.size() << endl;
    cout << "Front of the vector:" << v.front() << endl;
    cout << "Back of the vector: " << v.back() << endl;
    
    //v.clear();
    //cout<<"size of vector: " << v.size() << endl;
    // if(v.empty()) cout <<"Empty" << endl;
    // else cout<<"NOt empty" << endl;

    //v.erase(v.begin()+3);
    v.erase(v.begin()+3, v.end());

    for(int i = 0; i < v.size(); i++){
        cout << v[i] << "\t";
    }
 
    return 0;
} 
 */

#include<bits/stdc++.h>
using namespace std;


int main()
{
    int left = 0 , right = arr.size() - 1;

      while( left <= right){
        int middle = left(right - left)/2;
          
      }

    return 0;
}
